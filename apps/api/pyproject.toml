[tool.ruff]
line-length = 110
target-version = "py312"

[tool.ruff.lint]
select = ["E", "F", "I", "UP", "B"]
ignore = [
  "B008",  # FastAPI's Depends(...) as a default argument is the documented pattern
  "UP042", # (str, Enum) mixin, not StrEnum, for broadest SQLAlchemy Enum-type compatibility
]
